services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=todo_app_hexagonal'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=user'
    ports:
      - '5432:5432'
